create database banco_de_usuarios;
use banco_de_usuarios;

CREATE TABLE usuario (
    id INTEGER AUTO_INCREMENT PRIMARY KEY,
    nome VARCHAR(50) NOT NULL,
    email VARCHAR(50) NOT NULL UNIQUE,
    senha TEXT NOT NULL,
    cpf VARCHAR(11) NOT NULL UNIQUE,
    nivel INTEGER NOT NULL CHECK (nivel >= 0 AND nivel <= 3),
    status BOOLEAN NOT NULL
);


INSERT INTO usuario VALUES (null, 'adm', 'dddono@eEmail.com', 'password', '25354326746', 1, true);



select * from usuario; 

CREATE TABLE produtos (
    id INT AUTO_INCREMENT PRIMARY KEY,
    nome_produto VARCHAR(100) NOT NULL,
	valor DECIMAL(10, 2) NOT NULL,
    quantidade INT NOT NULL,
    avaliacao int NOT NULL,
    descricao VARCHAR(255) NOT NULL,
	status BOOLEAN NOT NULL,
    imagem longblob NOT NULL
);

drop table produtos;
select * from produtos; 

CREATE TABLE clientes (
    id INT AUTO_INCREMENT PRIMARY KEY,
    nome VARCHAR(50) NOT NULL,
    email VARCHAR(50) NOT NULL UNIQUE,
    senha VARCHAR(100) NOT NULL,
    cpf CHAR(11) NOT NULL UNIQUE,
    data VARCHAR(50) NOT NULL,
    genero VARCHAR(255) NOT NULL
);

select * from clientes; 
delete from clientes where id = 7;
drop table clientes;

CREATE TABLE endereco (
	id INT AUTO_INCREMENT PRIMARY KEY,
    id_cliente INT NOT NULL,
    cep VARCHAR(9) NOT NULL,
    logradouro VARCHAR(255) NOT NULL,
    numero VARCHAR(50),
    complemento VARCHAR(255),
    bairro VARCHAR(100) NOT NULL,
    cidade VARCHAR(100) NOT NULL,
    uf CHAR(2) NOT NULL
);
select * from endereco; 
INSERT INTO endereco VALUES (null ,10, '59868-965', 'Rua Carlo Dolci', '23', 'ali', "jardim", "São Paulo","SP");
drop table endereco;

CREATE TABLE carrinho (
    id INT AUTO_INCREMENT PRIMARY KEY,
    nome VARCHAR(50),
    valor DOUBLE NOT NULL,
    quantidade Int NOT NULL,
    imagem longblob NOT NULL
);

CREATE TABLE pedidos (
    id INT AUTO_INCREMENT PRIMARY KEY,
    valor DECIMAL(10, 2) NOT NULL CHECK (valor >= 0.00 AND valor <= 999999.99),
    nome_produto VARCHAR(255),
    quantidade INT NOT NULL CHECK (quantidade >= 0 AND quantidade <= 1000),
    cep VARCHAR(9) NOT NULL,
    logradouro VARCHAR(255) NOT NULL,
    numero VARCHAR(255),
    bairro VARCHAR(255) NOT NULL,
    cidade VARCHAR(255) NOT NULL,
    uf VARCHAR(2) NOT NULL,
    status VARCHAR(255) NOT NULL,
    id_cliente INT NOT NULL,
    metodoPag INTEGER NOT NULL CHECK (metodoPag >= 0 AND metodoPag <= 3)
);

select * from pedidos; 
drop table pedidos;